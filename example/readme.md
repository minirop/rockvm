`test.sl` has been compiled by my [Simple LISP](https://github.com/minirop/simple-lisp) compiler.

`test.bin` is the result and can be executed by RockVM.

`test.txt` is the output of `rockvm -d test.bin`.
